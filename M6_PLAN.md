# M6 — second logarithmic correction and a substantially smaller W threshold

Status: COMPLETE at the internal research-draft gate. Final audit:
results/m6-final-review.md. The second logarithmic theorem and structural
threshold reduction are accepted; a computationally useful low-index
threshold remains an unachieved optimization target, explicitly retained
in M6_ACCEPTANCE.md. The user accepted this research step after M5.
M5 remains frozen at76106cf; new work is on codex/w-second-order.

## Required outcomes

1. Prove or refute the conjectural additive constant of the directed series
   D, with a remainder strong enough to identify the second logarithmic
   correction uniformly on the W phase sector.
2. Derive and independently review
   theta_N=theta*-C1/logN+C2/log²N+O(log^-3N), or identify precisely any
   obstruction to this claimed order. Certify numerical enclosures of the
   new limiting coefficient; finite fitted constants are not proofs.
3. Substantially reduce the M5 sufficient W existence index2^(10^120) by
   sharper analytic bounds, targeting a useful and interpretable finite
   threshold. Merely changing the astronomical exponent is not the intended
   endpoint. Existence and uniqueness thresholds must keep distinct scopes.
4. Preserve failed conjectures and predictions. Freeze any numerical
   predictions before testing the new held-out index N1024. M5's N512 data
   is already observed and is not a new holdout.
5. Independently review each new proof and the final aggregation, freeze
   scientific sources before receipts, run appropriate verification, and
   complete a requirement-by-requirement final audit.

No acceptance of a partial result closes M6 while the second axis remains
unfinished. No new public publication or external contact is part of this
research step.

## Roles

Root owns the scope, second-order transfer, exact constant certificates,
integration and acceptance. Real Luna workers w_directed_ramps and
w_phase_probe own respectively the directed constant and effective product
bounds. Astra reviewer w_astra_review checks proof structure and all final
claims independently. Existing M5 sources and receipts are read-only.

## Initial leads

The hyperbolic directed sum suggests the additive constant
[log4-digamma(2+sqrt2)]/sigma. Its even parameter structure may yield a
better remainder than the previously conjectured O(e log1/e).

For the threshold, positivity of the v coefficients of U(t,v) implies
concavity of g_e(s), potentially yielding the exact bound R_e(s)<=1 and
removing the exp(10B) product estimate that dominates M5. This structural
claim and its quantitative consequences still require full review.
