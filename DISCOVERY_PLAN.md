# Active NEXT.md execution contract

Original plan frozen verbatim in environment/NEXT_GOAL_BRIEF.md at the start of
this goal. Previous goal completed M1 and its full-scope audit; this new goal
executes the next programme rather than redefining the M1 milestone.

Required work retained:
- E1 memory3…11, raw/orbit/equitable comparison, exact AP=PB, positive-vector
  lifting, out-of-sample continuation tests and rejected invalid partitions.
- E2 irreducibles indexed by length/span, n12…18 controlled dictionaries,
  bivariate renewal, span1 exact control, recurrence falsification/held-out terms.
- E3 primary small He bounds, rational anisotropic matrices, valid subgroup,
  unseen points, axis/symmetry/homogeneity and scope of convexity controls.
- M2 interface exposes lattice/domain/state/memory/motif/weight/bridge/width/
  symmetry choices with unsupported combinations rejected. Outputs include
  bounds/certificates/state size/time/memory/baseline and automatically generated
  observations, not a hard-coded parity assertion counted as discovery.
- Independent tester and Astra review; source-frozen experiments and hash receipts.
- Before any M3 claim: candidate selection, independent computation, attacks,
  proof interpretation and literature/novelty audit. M4 only if a result survives.

Root owns model targeting, integration, target-consistent comparisons and
scientific interpretation. Workers have explicitly disjoint file ownership.
All M1 artifacts remain historical; new computations get separate identifiers.
